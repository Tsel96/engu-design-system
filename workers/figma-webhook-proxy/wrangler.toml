name = "figma-webhook-proxy"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Secrets (set via wrangler CLI — never commit values here):
#   wrangler secret put GITHUB_PAT
#   wrangler secret put FIGMA_PASSCODE   # optional but recommended
